import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
  header: {
    fontSize: 22,
    color: '#4B595C',
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
